# Copy to .env and fill in the real key. Without OPENAI_API_KEY the server
# still starts and /health works — only /api/llm/test will fail at call time.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

SERVICE_NAME=ai-server
