# Copy to .env and fill in the real key. Without OPENAI_API_KEY the server
# still starts and /health works -- the LLM-backed branches of /api/assistant
# fail at call time instead.
#
# (`/api/llm/test` used to be the round-trip check named here. It was removed in
# ADR-0033: it had no auth dependency, so behind the proxy it was an open,
# unmetered OpenAI relay.)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.4-mini-2026-03-17

# Fallback provider behind the circuit breaker (ADR-0042). Leave it empty and
# there is simply no fallback -- the server logs that at startup rather than
# pretending to have one. It belongs HERE, not in the repo-root .env: the root
# file is for docker-compose, and the `ai` service loads this file via env_file,
# so this one location covers both local runs and the container.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

SERVICE_NAME=ai-server

# JWT verification (ADR-0023). This server only VERIFIES -- the Spring Boot
# backend is the sole issuer. HS256 is symmetric, so this must be the SAME value
# as `jwt.secret` on the backend (repo-root .env / JWT_SECRET). A mismatch is
# easy to misread: issuing succeeds and only this server returns 401.
JWT_SECRET=gimp_local_dev_secret_change_me_32b
JWT_ISSUER=gimp-backend
